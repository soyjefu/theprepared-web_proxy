worker_processes auto;

events {
  worker_connections 1024;
}

http {
  include       mime.types;
  default_type  application/octet-stream;

  sendfile        on;
  keepalive_timeout  65;
  client_max_body_size 100M;

  access_log /var/log/nginx/access.log;
  error_log /var/log/nginx/error.log;

  # 개발 중 접근 제한 (Basic Auth)
  auth_basic "Restricted";
  auth_basic_user_file /etc/nginx/.htpasswd;

  # cmhos.theprepared.kr → gunicorn :8002
  server {
    listen 80;
    server_name cmhos.theprepared.kr;

    location /static/ {
      alias /var/www/html/cmhos/static/;
    }
    location /media/ {
      alias /var/www/html/cmhos/media/;
    }
    location / {
      proxy_pass http://172.18.0.1:8002;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }
  }

  # asan.theprepared.kr → gunicorn :8001
  server {
    listen 80;
    server_name asan.theprepared.kr;

    location /static/ {
      alias /var/www/html/asan/static/;
    }
    location /media/ {
      alias /var/www/html/asan/media/;
    }
    location / {
      proxy_pass http://172.18.0.1:8001;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }
  }

  # cmw.theprepared.kr → gunicorn :8004
  server {
    listen 80;
    server_name cmw.theprepared.kr;

    location /static/ {
      alias /var/www/html/cmw/static/;
    }
    location /media/ {
      alias /var/www/html/cmw/media/;
    }
    location / {
      proxy_pass http://172.18.0.1:8004;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }
  }

  # ysw.theprepared.kr → gunicorn :8005
  server {
    listen 80;
    server_name ysw.theprepared.kr;

    location /static/ {
      alias /var/www/html/ysw/static/;
    }
    location /media/ {
      alias /var/www/html/ysw/media/;
    }
    location / {
      proxy_pass http://172.18.0.1:8005;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }
  }

  # noin.theprepared.kr → gunicorn :8003
  server {
    listen 80;
    server_name noin.theprepared.kr;

    location /static/ {
      alias /var/www/html/noin/static/;
    }
    location /media/ {
      alias /var/www/html/noin/media/;
    }
    location / {
      proxy_pass http://172.18.0.1:8003;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }
  }

  # --- 이전 설정 (비활성) ---

  # # soy.theprepared.kr → theprepared.kr 리디렉션
  # server {
  #   listen 80;
  #   server_name soy.theprepared.kr;
  #   return 301 http://theprepared.kr$request_uri;
  # }

  # # WordPress (theprepared.kr)
  # server {
  #   listen 80;
  #   server_name theprepared.kr www.theprepared.kr;
  #   root /var/www/html/wordpress;
  #   index index.php;
  #   location / {
  #     try_files $uri $uri/ /index.php?$args;
  #   }
  #   location ~ \.php$ {
  #     try_files $uri =404;
  #     fastcgi_split_path_info ^(.+\.php)(/.+)$;
  #     fastcgi_pass wordpress_app:9000;
  #     fastcgi_index index.php;
  #     include fastcgi_params;
  #     fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
  #     fastcgi_param PATH_INFO $fastcgi_path_info;
  #   }
  #   location ~ /\.ht { deny all; }
  #   location ~ \.user\.ini$ { deny all; }
  # }

  # # Account App (account.theprepared.kr)
  # server {
  #   listen 80;
  #   server_name account.theprepared.kr;
  #   location /static/ { alias /var/www/html/account/static/; }
  #   location /media/ { alias /var/www/html/account/media/; }
  #   location / {
  #     proxy_pass http://account_app:8000;
  #     proxy_set_header Host $host;
  #     proxy_set_header X-Real-IP $remote_addr;
  #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  #     proxy_set_header X-Forwarded-Proto $scheme;
  #   }
  # }

  # # Invest App (stock.theprepared.kr)
  # server {
  #   listen 80;
  #   server_name stock.theprepared.kr;
  #   location /static/ { alias /var/www/html/invest/static/; }
  #   location /media/ { alias /var/www/html/invest/media/; }
  #   location / {
  #     proxy_pass http://invest-app:8000;
  #     proxy_set_header Host $host;
  #     proxy_set_header X-Real-IP $remote_addr;
  #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  #     proxy_set_header X-Forwarded-Proto $scheme;
  #   }
  # }
}
